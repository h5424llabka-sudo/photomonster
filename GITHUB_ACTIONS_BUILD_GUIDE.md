# GitHub Actions を使用した Android APK ビルド & インストール完全手順書

本ドキュメントでは、**Android Studio やローカルの Java/Android SDK などの開発環境を PC に一切インストールせず**、GitHub のクラウド環境（GitHub Actions）を利用して PhotoMonster アプリ（APK ファイル）を自動ビルドし、実機（Galaxy Z Flip5 等）へインストールする全手順を解説します。

---

## 全体の流れ

```text
[事前準備] GitHub リポジトリ作成 & Google Maps API キーの Secrets 登録
    ↓
[コード反映] git push で GitHub Actions を自動トリガー
    ↓
[クラウドビルド] GitHub Actions の Ubuntu 仮想環境上で APK を自動ビルド（約2〜5分）
    ↓
[ダウンロード] GitHub の「Artifacts」から app-debug.zip をダウンロードして解凍
    ↓
[インストール] スマホ（Galaxy Z Flip5）に APK を転送してインストール
```

---

## ステップ 1: GitHub 上の事前準備（初回のみ）

### 1-1. リポジトリの作成
1. ブラウザで [GitHub - New Repository](https://github.com/new) にアクセスします。
2. 以下の設定を入力してリポジトリを作成します：
   - **Repository name**: `photomonster`
   - **Public / Private**: `Private`（非公開）を推奨
   - **Initialize this repository with**: すべてチェックなし（空の状態で作成）
3. 「**Create repository**」ボタンを押します。

### 1-2. Google Maps API キーを GitHub Secrets に登録
> [!IMPORTANT]
> Google Maps の API キーを Git コミットに含めると漏洩リスクがあるため、GitHub の機密情報管理（Secrets）機能を使用してビルド時にのみ安全に注入します。

1. 作成したリポジトリのページを開きます。
2. 上部タブの「**Settings**」をクリックします。
3. 左サイドバーの「**Secrets and variables**」 > 「**Actions**」をクリックします。
4. 「**New repository secret**」ボタンをクリックします。
5. 以下の通り入力し、「**Add secret**」をクリックして保存します：
   - **Name**: `MAPS_API_KEY`
   - **Secret**: `AIzaSy...`（お手持ちの Google Maps API キー）

---

## ステップ 2: ローカルリポジトリを GitHub へ Push する

PowerShell またはターミナルを開き、本プロジェクトのルートディレクトリ（`photomonster`）で以下のコマンドを実行します。

```powershell
# 1. プロジェクトフォルダに移動していることを確認
cd "c:\Users\c1370\OneDrive - Nikon\デスクトップ\業務外\photomonster"

# 2. リモートリポジトリの URL を設定（初回のみ。ユーザー名はご自身のアカウント名）
git remote add origin https://github.com/<あなたのGitHubユーザー名>/photomonster.git

# 3. 最新状態をコミット・プッシュ
git push -u origin master
```
※ 認証を求められた場合は、ブラウザ認証または GitHub Personal Access Token (PAT) を使用してください。

---

## ステップ 3: GitHub Actions による自動ビルドの確認

1. ブラウザで GitHub リポジトリを開きます。
2. 画面上部の「**Actions**」タブをクリックします。
3. 一覧の一番上に、プッシュしたコミット名（または "Build APK"）のワークフローが表示されます。
   - 🟡 **黄色の回転アイコン**: ビルド実行中（初回は約3〜5分、キャッシュがある次回以降は約1〜2分）
   - 🟢 **緑色のチェックマーク（✔）**: ビルド成功！
   - 🔴 **赤色のバツマーク（✖）**: エラー発生（クリックしてログ詳細を確認可能）

> [!TIP]
> コードを変更せずに再度ビルドしたい場合は、左メニューの「**Build APK**」 > 右側の「**Run workflow**」ボタンから手動実行も可能です。

---

## ステップ 4: APK ファイルのダウンロード

ビルドが完了（🟢）したら、生成された APK を取得します。

1. 「Actions」一覧から、成功したワークフロー（緑のチェックマークの行）をクリックして詳細画面を開きます。
2. ページを一番下までスクロールします。
3. **「Artifacts」**（成果物）エリアに表示されている **`app-debug`** をクリックします。
4. ブラウザで `app-debug.zip` のダウンロードが開始されます。
5. ダウンロード完了後、zip ファイルを解凍すると中に **`app-debug.apk`** が入っています。

---

## ステップ 5: Galaxy Z Flip5 へのインストールと初期設定

### 5-1. スマホへ APK を転送する
以下のいずれかの方法で `app-debug.apk` を Galaxy Z Flip5 に送ります：
- **USB 接続**: PC とスマホをケーブルで繋ぎ、スマホの「ダウンロード」フォルダ等にコピーする
- **クラウドストレージ**: Google ドライブ、OneDrive 等にアップロードし、スマホ側でダウンロードする
- **クイック共有 / ニアバイシェア / メール等**

### 5-2. 提供元不明のアプリの許可（初回のみ）
Galaxy のセキュリティ設定により、Google Play 以外からのアプリ導入には許可が必要です。

1. スマホの「**マイファイル**」（またはファイル管理アプリ）を開きます。
2. コピー/ダウンロードした `app-debug.apk` をタップします。
3. 「セキュリティ上の理由から、この提供元からの不明なアプリをインストールすることはできません」と表示された場合：
   - 「**設定**」をタップ
   - 「**この提供元のアプリを許可**」を **ON** に切り替え
   - 戻るボタンを押す
4. 「**インストール**」をタップしてインストールを完了させます。

> [!IMPORTANT]
> 以前のバージョン（v1.0 など）が既に端末に入っている場合、権限変更（`ACCESS_MEDIA_LOCATION`）の適用を確実にするため、**古いアプリをアンインストールしてから新規インストール**することをおすすめします。

---

## ステップ 6: アプリの動作確認と使い方

1. アプリ一覧から **PhotoMonster** を起動します。
   - 画面上部タイトルに **`v1.2`** と表示されていることを確認（最新バージョンが適用されている目印です）。
2. 右上の「**写真を選択**」ボタンをタップします。
3. **初回のみ「位置情報へのアクセス（写真・動画内の位置情報）」の許可ダイアログ**が表示されるので、「**許可**」を選択します。
4. ギャラリー／ファイルピッカーから Galaxy Z Flip5 で撮影した写真を複数選択します。
5. 選択が完了すると、EXIF から緯度経度が抽出され、Google Maps 上にピンが表示されます。
   - ピンをタップすると撮影日時・住所・サムネイルカードが表示されます。
   - 下部のサムネイル一覧からタップして該当地点へジャンプできます。

---

## トラブルシューティング

| 現象 / エラー | 原因 | 対処法 |
|---|---|---|
| **Actions で `MAPS_API_KEY` エラーまたはマップが真っ白** | GitHub Secrets に API キーが未登録、または Google Cloud 側でキーが無効 | リポジトリの `Settings > Secrets and variables > Actions` に `MAPS_API_KEY` が正しく登録されているか、および Google Cloud Console で「Maps SDK for Android」が有効化されているか確認してください。 |
| **Actions ビルドが失敗する（赤✖）** | ソースコードのエラー、あるいは Gradle の設定不備 | 失敗したワークフローをクリックし、ログの「Build Debug APK」ステップを開いてエラー内容を確認してください。 |
| **アプリで「位置情報が含まれていません」と出る** | カメラ撮影時に位置情報付与がOFF、または権限が不許可 | スマホのカメラ設定で「位置情報タグ」が有効になっているか、およびアプリに「メディアの位置情報」権限が許可されているか端末の設定画面から確認してください。 |
